{
  "name": "wipvalidation",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based diff and validation tools for ServiceTitan WIP report exports",
  "scripts": {
    "test": "node --test 'tests/*.test.js'"
  },
  "engines": {
    "node": ">=18"
  }
}
